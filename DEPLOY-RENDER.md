BaBuLo Play v10.1 - Render

IMPORTANTE: no GitHub, os arquivos desta pasta devem ficar na raiz do repositório.
O package.json precisa estar no mesmo nível de apps/ e packages/.
No Render, Root Directory deve ficar vazio (raiz do repositório).
Build API: npm install --no-audit --no-fund && npm run build:api
Build Web: npm install --no-audit --no-fund && npm run build:web
