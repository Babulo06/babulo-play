# BaBuLo Play V10.4.23 — Correção do Perfil do Artista

- Corrigido erro de build no `apps/web/app/page.tsx` causado por `setProfileOpen` não declarado em `ArtistWorkspace`.
- Separado o acesso ao `Perfil do artista` do `Perfis nas plataformas`.
- O painel do artista agora abre o `Perfil do artista` e o mapeamento de plataformas em modais separados.
- Mantido o fluxo de perfil central e sincronização preparado na V10.4.23.
